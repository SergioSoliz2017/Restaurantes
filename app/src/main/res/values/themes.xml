<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema Base SIN ActionBar -->
    <style name="Base.Theme.Restaurantes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Personaliza tu tema aquí (colores, tipografía, etc.) -->
        <!-- Ejemplo de personalización de colores:
        <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema Base CON ActionBar -->
    <style name="Base.Theme.Restaurantes.WithActionBar" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">#FF6B6B</item> <!-- Color del tema -->
        <item name="colorOnPrimary">#FF6B6B</item>
    </style>

    <!-- Tema heredado, en caso de que quieras usar un nombre común para tus temas base -->
    <style name="Theme.Restaurantes" parent="Base.Theme.Restaurantes" />

    <!-- Personalización para un indicador activo en BottomNavigationView -->
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">#fff7e1</item>
    </style>

</resources>